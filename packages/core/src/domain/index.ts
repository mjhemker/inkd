// Pure, server-safe domain logic (no React hooks). Importable from server
// components via the `@inkd/core/domain` subpath without dragging the client
// hooks barrel into a server bundle.
export * from "./onboarding";
export * from "./shops";

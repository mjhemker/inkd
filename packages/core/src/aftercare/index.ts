/** Aftercare pure domain logic: schedule generation, copy, and the consent /
 * healed-photo share state machine. Platform-neutral (no I/O). */
export * from "./schedule";
export * from "./consent";
